name: test-deploy

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore ./deploy-test.sln

      - name: Build
        run: dotnet build ./deploy-test.sln --configuration=release --no-restore

      - name: Pubish
        run: dotnet publish ./deploy-test.sln --configuration=release --no-build
